# Get a key at https://openweathermap.org/api (free tier is enough for local dev).
# Copy this file to `.env` and fill in the value. Never commit `.env`.

OPENWEATHER_API_KEY=

# Optional: port for the local dev server (default 3000)
# PORT=3000
