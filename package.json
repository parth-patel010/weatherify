{
  "name": "weatherify",
  "private": true,
  "description": "Weather app with OpenWeatherMap proxy (API key in server env only)",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
